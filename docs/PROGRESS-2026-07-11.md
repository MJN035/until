# 세션 리포트 — 2026-07-11 (Fable 자율 루프 재개)

> 이전 종합: `PROGRESS-2026-07-03.md`. 이 문서는 7/11 세션(v1.3.0~v1.5.0)의 요약.

## 한눈에

| 항목 | 수 |
|---|---|
| 릴리스 | 6개 (v1.3.0 → v1.7.1) |
| 게이트 리뷰 | 4회 (10회차: 에이전트 3 / 11·12회차: 에이전트 1 / 13회차: 에이전트 2) |
| 릴리스 게이트 워크스루 | 2회 (14체크 / 업로드 포함 10체크) |
| 실버그 수정 | 22건 (10회차 8 + 11회차 3 + 12회차 4 + 13회차 7) |
| 테스트 | 25스위트 유지, 회귀 케이스 35+ 추가 |

## 릴리스별

### v1.3.0 "한 번에, 간단하게"
v1.2.0 이후 미검증 상태였던 7커밋(간단 모드 `/simple`, 클로드 리테마, 홈 초미니멀,
기한 필터, ⚡ 바로 초안, billing 스캐폴드, 히스토리 엔티티 정화)을 **게이트 리뷰
10회차**로 검증 — 실버그 8건 수정:
- billing: 비-UTF8 `license.txt`(PowerShell UTF-16 저장)가 전 페이지 500 연쇄 크래시,
  적립 경합 유실(잠금+원자 교체+Windows 백신 잠금 재시도).
- answer_history: record+load **이중 unescape**로 원본 2단 훼손 → 정화 load 일원화,
  완전 엔티티만 1패스 치환(부분·제어문자·무세미콜론 보존), 대문자 X.
- 웹: /simple 에러 막다른 링크, fast 폴백이 제출완료 과제 조용히 초안(+한도 차감),
  공백 env 토큰 판정 모순.
- 러너 미등록 테스트 2건 발견·등록.

### v1.4.0 "첨부까지 읽는, 간단하게 끝내는"
- 간단 모드 마무리: 준비 점검 ⚠ 한 줄·🕘 지난 답 칩·/plan 실패 피드백·/sessions ✳.
- **docx/pptx/html 내장 폴백 파서**(의존성 0) — zip 바이트 오염 방지, 이진 포맷 경고.
- test_runners AST 등록 감사(등록 누락 유형 영구 차단).
- 게이트: **웹 전 경로 워크스루 14체크 전부 통과**(간단 3단 왕복·다운로드·플랜 변형·
  에러 경로·XSS 스모크).

### v1.5.0 "워드로 내고, 한글도 읽는"
- **제출용 .docx 내보내기**(의존성 0 OOXML) — CLI·웹·간단 모드·demo 전 표면.
- **HWPX 첨부 파싱**(의존성 0) — 한국 대학 필수 포맷.
- 인박스 D-day 태그(+임박 강조)·60초 캐시, 답 입력 localStorage 보존.
- 게이트 리뷰 11회차 → D-day UTC/로컬 달력 오차 등 3건 수정.

### v1.6.0 "내 자료까지 함께"
- **웹 내 자료 파일 첨부** — /simple multipart 업로드(의존성 0 email 파서), `[내 자료]`
  SourceDoc 주입 → `[자료N]` 인용, 실패·빈 파일은 경고. 25MB 상한.
- **게이트 리뷰 12회차**(재현 스크립트 20여 케이스) → 4건 수정: 413 응답이 연결
  리셋으로 유실되던 것(드레인 후 응답), 중복 파일명 접미사, 0바이트 경고, 센티널 제거.

### v1.7.0 "내 말투까지 함께"
- **웹 '내가 쓴 글' 업로드 → 문체 반영**(voice_files, docx/hwpx 변환) — 개인화 웹 완성.
- 릴리스 게이트: 업로드·문체 포함 워크스루(에이전트) 통과.

### v1.7.1 패치 — 종합 리뷰 13회차 (에이전트 2, 실서버·재현 스크립트)
- web.py 전체: 게이트 303이 업로드 중 RST로 유실(드레인), **재방문 프리필이 사용자
  수정 답을 AI 제안으로 되돌리던 실측 버그**(내 답 우선), 간단 모드 프리필, 예외 튜플.
- 파서: .docx C0 제어문자 → Word 못 여는 파일, HTML close() 꼬리 유실, zip 폭탄
  50MB 상한. 깨진 아카이브 27케이스 수렴 확인.

## 운영 메모
- 태그 v1.3.0~v1.5.0은 로컬 — 원격 반영은 `git push origin main --tags`(사용자 승인).
- `run_tests.py` 간헐 실패 시 Windows 백신의 새 파일 잠금 가능성부터 의심
  (billing 원자 교체에서 실제 발생 → 재시도 로직으로 해소).
- 남은 외부 자원 백로그: GEPA 유료 TPM 대예산 실행, SSO 라이브 재검증(브라우저 클릭).
