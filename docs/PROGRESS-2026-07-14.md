# 세션 종합 — 2026-07-14 "구름 위로, 안전하게" (v1.8.0)

사용자 지시: 새 플러그인·스킬을 활용해 until의 단계를 올릴 것.
방향(사용자 선택): ① 제품을 세상에(호스팅 전환+런칭) → ② 프로덕션 품질 → ③ 디자인.
배포 형태: **Cloudflare 호스팅 웹 서비스**.

## 릴리스
- **v1.8.0 "구름 위로, 안전하게"** — 로컬 전용 → Until Cloud(호스팅 멀티유저 베타).

## 트랙 A — 런칭 (완료, 배포 클릭만 남음)
- 설계 스펙: Containers 채택(Python Workers는 동기 HTTP 불확실) —
  `docs/superpowers/specs/2026-07-14-until-cloud-design.md`
- 클라우드 모드 코어: uid 쿠키 격리·베타 게이트·보안 헤더·healthz·전역 상한
- KV 백킹(`until/cloudkv.py`): 재시작 생존(미러+하이드레이션), FIFO 직렬화
- 배포 패키징 `deploy/`: Dockerfile(스모크 2회), Containers Worker, 랜딩 타깃,
  DEPLOY.md + **원커맨드 `deploy/deploy.ps1`**
- 마케팅: 포지셔닝(`.agents/product-marketing.md`), 랜딩 페이지(경계선 중심 카피,
  앱과 토큰 공유), 런칭 플랜(`docs/launch/` — 채널별 초대 코드 측정)
- **남은 것:** 사용자 `npx wrangler login` + Workers Paid 활성화 → `deploy/deploy.ps1`

## 트랙 B — 프로덕션 품질 (완료)
- **보안 리뷰 14회차**(위협모델 10항목): 신뢰도 0.7+ 취약점 **0건**.
  심층방어 1건 반영 — 인메모리 세션 소유자(_OWNER) 추적.
- **멀티에이전트 감사 14회차**(Workflow: 파인더 5 + 발견당 반박 검증 2, 총 27에이전트,
  ~193만 토큰): 반박 표결 전원 확정 **실버그 10건** → 전부 수정.
  - HIGH 3: KV 히스토리 삭제/절단(미확정 미러), _HYDRATED 조기 마킹 고착,
    /pick·/collect 운영자 토큰 폴백
  - MED/LOW 7: 프루닝 KV 미삭제, 전역 상한 미지속, CSP 폰트 차단, mtime 왜곡,
    미러 순서 비보장, GET /beta 404, 클라우드 /plan 막다른 폼
- **클라우드 워크스루 15체크 전부 통과**(에이전트, 실서버 2사용자 격리 검증).
- 테스트 25→**26스위트**(test_cloud 8케이스: 격리·게이트·KV 생존·장애 시나리오).

## 트랙 C — 디자인 (완료)
- `docs/DESIGN.md`: 토큰 3계층(원값/용도/컴포넌트) 단일 소스 + 접근성 유지 조건.
- 랜딩 다크 토큰을 앱과 통일(드리프트 제거), 폰트 로딩 사실관계 정정.
- 클라우드 모드 안내 문구 정합("이 컴퓨터" → 모드별 정확한 표현).

## 숫자
- 커밋 14개, 릴리스 1개(v1.8.0), 리뷰 게이트 2회(보안+감사), 실버그 10건 수정,
  워크스루 15체크, 26스위트(~10초).

## 다음 후보
- 배포(사용자 클릭) → 랜딩 CTA 연결 → 런칭 플랜 1단계(친구 5명)
- 원격 푸시: `git push origin main --tags` (v1.3.0~v1.8.0 미푸시)
- GEPA 유료 티어 실험, Python Workers 무료 포트(JSPI 안정화 후)
