# 2026-07-03 자율 개발 세션 리포트

> Claude(Fable) 자율 루프가 하루 동안 진행한 작업 요약. 협업자 온보딩·리뷰용.
> 모든 변경은 "작은 증분 → 23스위트 오프라인 테스트 통과 → 커밋" 사이클로 진행됨.

## 한눈에

| 항목 | 수치 |
|---|---|
| 커밋 | 115+ (기능/개선 46 · 버그 수정 7 · 문서/예제 나머지) |
| 릴리스 | **v0.2.0 → v0.7.0** (6개, 로컬 태그) |
| 테스트 | 15 → **23스위트** (러너 병렬화: 2분+ → ~5초) |
| 품질 리뷰 | 다중 에이전트 리뷰 **7회** → 재현 확인 실버그 **17건 수정** |

## 릴리스별 요약

- **v0.2.0 "제출 직전 점검"** — 제출용 내보내기(.md/.html/인쇄), 분량·마감 D-day·인용
  커버리지·요건 체크리스트를 하나의 '준비 점검'으로(CLI·웹·문서·JSON API 공유), 유형별 조정.
- **v0.3.0 "기억하는 조수"** — 결정 답변 히스토리(지난 답 재제안, 과매칭 3중 게이트),
  히스토리 인지 AI 제안, 결정 근거(왜 당신 몫인지) 전 표면, diffview(변경 투명화).
- **v0.4.0 "오리엔테이션"** — 결정 진행률(M/N), 과제 제목 헤더, /sessions(목록·검색·삭제·
  경고 배지·유형 배지), 세션 지속화(재시작 복원)+메타 캐시.
- **v0.5.0 "내 문체 그대로"** — 내 답 종결어미를 AI 제안·finalize에 연계, 결정 성격별
  반영(블렌딩) 지침, voice 합니다체 판정 버그 수정.
- **v0.6.0 "마감을 알아듣는"** — 상대 날짜(내일/모레/요일/이번·다음 주), 히스토리 요약
  명령(투명성), '오늘날'·'내일모레' 관용어 처리.
- **v0.7.0 "마감 이해 고도화"** — 연장 공지(가장 늦은 날짜), 과거 참조 날짜 구분,
  연말 걸침 연장, 웹 오류 경로 테스트. (+0.8.0 진행 중: 마감 시각 병기)

## 품질 리뷰(다중 에이전트)에서 잡은 대표 버그

- 마감 파서가 소수·버전·문제번호(3.11, 문제 2.1)를 날짜로 오인 → 문맥 게이트.
- "최대 3000자"가 하한으로 뒤집혀 분량 초과를 '충족'으로 오판 → 선행 수식어+min/max 병합.
- '장' 챕터('교재 5장')를 페이지 분량으로 오인 → 분량 문맥 필수.
- 히스토리 과매칭(상투 어미 '…를 정하세요'가 유사도 지배) → 성격+내용어 게이트.
- 세션 캐시가 D-day 경고를 날이 지나도 스테일 유지 → 날짜 포함 캐시 키.
- cp949(메모장) 한국어 파일 파싱 전체 실패 → 인코딩 폴백.
- 연장 공지에서 옛 마감이 새 마감을 이기는 3가지 경로 → 스팬 중복 제거·비대칭 통일·연말 걸침.

## 원칙(전 작업 공통)

- **경계선**: 어떤 기능도 사람 판단을 대신 확정하지 않음(제안·표시·판정만).
- **결정적 우선**: 신규 점검·파서 전부 LLM 0(토큰 비용 없음, 오프라인 테스트 가능).
- **개인정보**: 히스토리·세션은 로컬 `_until_work/`(gitignore)에만, 확인·삭제 수단 제공.

## 🏁 1.0.0 도달 (당일 저녁 갱신)

같은 날 저녁 **v1.0.0 "경계선까지, 완성"** 릴리스 — 최종 수치:
릴리스 **9개**(v0.2.0~v1.0.0), 게이트 리뷰 **8회**+광역 스모크 **90케이스**
(파서 70+유형 분류 20, 오분류 0), 재현 확인 실버그 **22건 수정**, 커밋 **130+**,
**웹 전 경로 워크스루 67체크 통과**. 1.x에서 웹 /history(개인정보 통제)·
JSON severity 정렬 추가.

## 다음 후보 (0.8.0~ 당시 기준, 위 1.0.0에 상당수 반영됨)

세션 내보내기/가져오기, readiness 연장 감지 표시, GEPA 라이브 최적화(유료 티어 필요),
SSO 라이브 재검증. 상세는 `CLAUDE.md` 하단·`CHANGELOG.md` 참조.
