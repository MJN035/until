# CHANGELOG — Until (코어)

이 파일은 **공개 코어 저장소**의 변경 이력이다.

Until은 2026년 상반기에 비공개로 개발됐고, 그 기간의 상세 이력(600여 커밋 분량)은
팀 내부 맥락 — 코퍼스를 제공한 팀원들의 실명, 실제 수강 과목, 개별 과제 판정 기록 —
과 얽혀 있어 공개 저장소에 옮기지 않았다. **제3자의 학업 데이터를 공개하지 않는다는
원칙**이 커밋 이력의 완결성보다 우선한다.

아래는 공개 시점의 기능 상태이며, 무엇이 어디에 있고 어떻게 검증되는지는
[`docs/FEATURES.md`](docs/FEATURES.md)가 한 장으로 보여 준다.

---

## [1.10.0] — 2026-08-23 · 코어 공개

첫 공개 릴리스. AGPL-3.0으로 코어를 공개한다.

### 이 저장소에 있는 것

- **Capture** — eTL(Canvas/Moodle) 과제 수집, 첨부 파싱(pdf·docx·hwp·hwpx·zip),
  수업자료·공지 연결
- **Understanding** — 과제 유형 라우팅, 요구사항 추출, 골격 생성, 마감·분량·인용 해석
- **Execution** — 경계선 초안 생성, BoundaryGuard(재요청 루프), 톤·페르소나 반영
- **Boundary** — `[[DECISION]]` 마커로 사람 판단 지점을 남기고 대신 확정하지 않는다
- **제출 준비** — readiness 점검, 양식 채움, 제출 파일 내보내기
- **웹 UI** — stdlib 서버(`until.web`)와 ASGI(`until.asgi`) 두 표면
- **CLI** — `python -m until <파일>`, `demo.py`

키·인터넷 없이 mock 백엔드로 전부 돌아가고, **88개 오프라인 스위트**가 이를 고정한다.
파이프라인은 결정적이다(`tools/check_determinism.py`).

### 이 저장소에 없는 것

서버·결제·관리자 계층은 비공개로 남는다 — 과금·크레딧 원장, PG 웹훅, 관리자 보드,
운영 KV 미러, 소셜 로그인(Google·Kakao), 방문 분석, 베타 초대 접수.

해당 모듈들은 **인터페이스만 유지하는 무동작 스텁**으로 들어 있다
(`until/billing.py`·`cloudkv.py`·`adminboard.py`·`google_auth.py` 등). 코어는 그것들
없이 단독으로 동작하며, 운영 배포가 같은 이름의 실제 구현으로 교체한다.

의존 방향은 한쪽으로만 흐른다 — **비공개 서버 → 코어(허용), 코어 → 비공개 서버(금지).**
`tests/test_license_boundary.py`가 이 방향을 강제한다.

### 라이선스

AGPL-3.0-only. 네트워크 조항(§13)에 따라 이 소프트웨어를 수정해 네트워크 서비스로
제공하면 그 이용자에게 수정된 소스를 제공해야 한다. 자기 학습·연구·사내 사용에는
아무 의무가 없다. 자세한 내용은 [`LICENSE`](LICENSE)와 [`NOTICE`](NOTICE).
